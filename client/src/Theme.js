import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            light: "#f6921a",
            main: "#f6921a",
            dark: "#c9730b",
            contrastText: "#fff",
        },
    },
});

export default theme;
